<?xml version="1.0" encoding="utf-8"?>
<!--
  Sử dụng CoordinatorLayout làm gốc
  để FloatingActionButton (FAB) hoạt động mượt mà khi cuộn.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".features.one_accounts.ui.AccountFragment">

    <!--
      RecyclerView để hiển thị danh sách các tài khoản.
      app:layout_behavior dùng để báo cho FAB biết khi nào cần "né" (scroll)
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_accounts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:listitem="@layout/item_account" />

    <!--
      FloatingActionButton (FAB) để thêm tài khoản mới.
    -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_account"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="Thêm tài khoản mới"
        app:srcCompat="@drawable/ic_add" />

    <!-- (Tùy chọn) TextView để hiển thị khi danh sách trống -->
    <TextView
        android:id="@+id/tv_empty_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Chưa có tài khoản nào. \nHãy nhấn '+' để thêm."
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        android:visibility="gone"
        tools:visibility="visible" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
